// Club badges - Deshabilitado (solo texto)

// Función para generar el HTML del club (solo texto)
function renderClubBadge(clubName) {
    return '';
}

// Función vacía para compatibilidad
function waitForBadges() {
    return Promise.resolve();
}
